class SetConfigData {
  final int masterId;
  final int frequency;
  final int mainFrequency;
  final bool isMain;
  final int clubId;

  SetConfigData({
    required this.masterId,
    required this.frequency,
    required this.mainFrequency,
    required this.isMain,
    required this.clubId,
  });
}
